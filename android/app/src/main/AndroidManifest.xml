<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE scan + connect. SCAN/CONNECT are the Android 12+ (API 31) model;
         neverForLocation says we don't derive location from scan results. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Legacy BLE permissions for API <= 30. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Location is for the map (geotagging fixed-device detections + the "you are
         here" dot), not the scan. Android 12+ silently ignores a FINE request that
         does not also ask for COARSE, so both must be declared. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- osmdroid tiles -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Haptic alerts for stealth mode. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Drive mode: a foreground service that posts the glanceable detection-counter
         notification and keeps the BLE link alive in the background. connectedDevice is the
         FGS type for a held GATT link; POST_NOTIFICATIONS is the Android 13+ runtime grant. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 16 (API 36): post a promoted ongoing notification = the Live Update status-bar chip. -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- allowBackup=false: the detections file (MACs + capture GPS + timestamps) and the
         ignore-list prefs must not flow to cloud Auto-Backup or device-transfer. The
         Keystore-wrapped buffer-key blob is safe, but no app data should leave the device. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OuiAcab">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OuiAcab">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Shares the exported detections CSV from cacheDir via a content:// uri. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Drive-mode foreground service: the glanceable detection-counter notification. -->
        <service
            android:name=".ble.AcabLinkService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
